<Window x:Class="Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calculator"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <TextBlock Text="Sistema de Gestão" Background="LightGray"
	    Grid.Row="0" 
	    Grid.Column="0" 
	    Grid.ColumnSpan="2"/>

        <StackPanel Grid.Row="1" Grid.Column="0" Background="#EEE">
            <Button Content="Início"/>
            <Button Content="Configurações"/>
        </StackPanel>

        <Border Grid.Row="1" Grid.Column="1" Background="White">
            <TextBlock Text="Área de Trabalho"/>
        </Border>

        <TextBlock Text="Status: Pronto" Background="Gray" Foreground="White"
	    Grid.Row="2" 
	    Grid.Column="0" 
	    Grid.ColumnSpan="2"/>
    </Grid>
</Window>
